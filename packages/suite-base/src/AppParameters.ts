export enum AppParametersEnum {
  // Used to start with a specific layout passed as parameter
  DEFAULT_LAYOUT = "defaultLayout",

  // Used to start in a specific timestamp passed as parameter
  TIME = "time",
}
